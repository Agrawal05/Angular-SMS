import { DisableKeyboardDirective } from './disable-keyboard.directive';

describe('DisableKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
